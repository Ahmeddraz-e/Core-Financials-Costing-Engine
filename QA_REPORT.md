
# LODing ERP — تقرير فحص الجودة الشامل (QA Report)

**تاريخ التقرير:** 2026-07-11  
**المراجعة:** كود كامل للمشروع (Full Codebase Review)  
**حالة البناء:** Build ✅ (يمر بنجاح، الخطأ الوحيد موجود مسبقًا في Purchases.tsx:153)

---

## جدول المحتويات
1. [خلاصة تنفيذية](#1-خلاصة-تنفيذية)
2. [مشاكل حرجة (CRITICAL)](#2-مشاكل-حرجة-critical)
3. [مشاكل عالية الأولوية (HIGH)](#3-مشاكل-عالية-الأولوية-high)
4. [مشاكل متوسطة (MEDIUM)](#4-مشاكل-متوسطة-medium)
5. [مشاكل منخفضة (LOW)](#5-مشاكل-منخفضة-low)
6. [مشاكل التصميم (UI/UX)](#6-مشاكل-التصميم-uiux)
7. [سيناريوهات الاختبار اليدوي](#7-سيناريوهات-الاختبار-اليدوي)
8. [فحص التصديرات (Excel / PDF / Print)](#8-فحص-التصديرات-excel--pdf--print)
9. [فحص حفظ البيانات (Persistence)](#9-فحص-حفظ-البيانات-persistence)
10. [ملحق: العيوب المحاسبية والحسابية الكبرى](#10-ملحق-العيوب-المحاسبية-والحسابية-الكبرى-الواقع-المالي-للمطاعم)

---

## 1. خلاصة تنفيذية

### ما تم فحصه
- ✅ جميع مكونات React (27 ملفًا)
- ✅ جميع ملفات الأدوات (excelExport, pdfExport, printUtils)
- ✅ طبقة قاعدة البيانات SQLite (db.ts, schema.ts, seed.ts, 15 query file)
- ✅ طبقة API (api.ts, 10 route files)
- ✅ الترخيص (licensing)
- ✅ إدارة الحالة المركزية في App.tsx
- ✅ جميع التصديرات Excel + PDF + Print

### النتائج
| المستوى | العدد | الحالة |
|---------|-------|--------|
| 🔴 CRITICAL | 5 | تحتاج إصلاح فوري |
| 🟠 HIGH | 9 | تحتاج إصلاح |
| 🟡 MEDIUM | 12 | يفضل إصلاحها |
| 🔵 LOW | 15 | تحسينات مستقبلية |
| 🎨 UI/UX | 8 | تحسينات واجهة |

---

## 2. مشاكل حرجة (CRITICAL)

### C1. تصدير فواتير المبيعات — الشيت الثاني فارغ
- **الملف:** `src/utils/excelExport.ts` — السطور 851-878
- **الوصف:** دالة `exportSalesInvoicesExcel` تنشئ sheet ثاني (`ws2` / "Invoice Details") بعناوين الأعمدة لكن **لا تضيف أي بيانات** إليه. الش sheet يظهر فارغًا دائمًا.
- **التأثير:** المستخدم يرى شيت فارغ باسم "تفاصيل الفواتير"
- **الحل:** إما إزالة الـ sheet الثاني، أو إضافة بيانات الأصناف إليه.

### C2. تصدير فواتير المبيعات — `colCount` غير متطابق
- **الملف:** `src/utils/excelExport.ts` — السطر 777
- **الوصف:** `colCount = 15` لكن العناوين والقيم هي 14 فقط. ينتج عمود 15 فارغ.
- **التأثير:** عمود إضافي فارغ في ملف Excel
- **الحل:** تغيير `colCount` إلى 14.

### C3. دوال Debug مكشوفة بدون مصادقة (ثغرة أمنية)
- **الملف:** `src/routes/auth.ts` — السطور 10-42
- **الوصف:** المسارين `GET /api/auth/debug-users` و `POST /api/auth/debug-login` بدون أي مصادقة. الأول يرجع جميع المستخدمين مع hashes الباسوورد.
- **التأثير:** أي شخص يمكنه الوصول للـ API يمكنه سرقة بيانات المستخدمين
- **الحل:** إزالة هذه الـ endpoints أو حمايتها بـ `if (process.env.NODE_ENV !== 'production')`.

### C4. استرجاع النسخة الاحتياطية يغلق قاعدة البيانات بدون ضمان إعادة التشغيل
- **الملف:** `src/routes/system.ts` — السطور 247-260
- **الوصف:** دالة `handleRestore` تنفذ `db.close()` ثم تنسخ الملف ثم تستدعي `app.relaunch()` أو `process.exit(0)`. إذا فشلت إعادة التشغيل، السيرفر يبقى بقاعدة بيانات مغلقة.
- **التأثير:** تعطل كامل للتطبيق بعد استرجاع نسخة احتياطية
- **الحل:** استخدام `db.backup()` API من better-sqlite3 بدلاً من إغلاق القاعدة.

### C5. النسخ الاحتياطي يستخدم `fs.copyFileSync` على SQLite مباشر
- **الملف:** `src/routes/system.ts` — السطر 228
- **الوصف:** `fs.copyFileSync` ينسخ ملف SQLite أثناء تشغيله (WAL mode). هذا غير آمن—الملف قد يكون غير متسق.
- **التأثير:** نسخة احتياطية تالفة محتملة
- **الحل:** استخدام `VACUUM INTO` أو `db.backup()` API.

---

## 3. مشاكل عالية الأولوية (HIGH)

### H1. فشل الحفظ بدون إبلاغ المستخدم
- **الملف:** `src/App.tsx` — السطور 275-279
- **الوصف:** دالة `updateErpState` تمسك أخطاء الحفظ بـ `.catch(err => console.error(...))` فقط. المستخدم لا يرى أي إشعار بفشل الحفظ.
- **سيناريو الاختبار:** افصل الإنترنت/السيرفر ثم أجرِ تغييرًا. ستظهر الواجهة التغيير لكنه لم يُحفظ.
- **الحل:** إظهار `window.showAlert()` عند فشل الحفظ وإعادة المحاولة.

### H2. تصديرات Excel بدون try/catch (4 مواقع)
- **الملف:** `src/components/Treasury.tsx` — السطور 1014, 1038, 1140  
- **الملف:** `src/components/Sales.tsx` — السطر 990
- **الوصف:** دوال `onClick` في أزرار التصدير تستخدم `await` بدون `try/catch`. إذا فشلت عملية الكتابة (مثلاً خطأ في ExcelJS)، يحدث `Unhandled Promise Rejection`.
- **الحل:** إضافة `try/catch` مع `window.showAlert` عند الفشل.

### H3. `onAddMoneyTransaction` — Stale Closure في Purchases
- **الملف:** `src/App.tsx` — السطر 710
- **الوصف:** دالة `onAddMoneyTransaction` هي inline closure تلتقط `erpData` من الريندر الحالي. إذا أضيفت حركتين ماليتين بسرعة، قد تفقد إحداهما.
- **ساناريو الاختبار:** في Purchases، أنشئ حركتين شراء بسرعة متتالية. تحقق من عدم فقدان الحركات.
- **الحل:** استخدام functional updater: `updateErpState(prev => ({...prev, moneyTransactions: [tx, ...prev.moneyTransactions]}))`.

### H4. مصطلح `exportToCSV` لا يزال مستخدمًا مع XSS
- **الملف:** `src/utils/printUtils.ts` — السطر 545
- **الوصف:** بيانات المستخدم تُدرج في HTML بدون escaping. لو أي خانة تحتوي `<script>`، سيتم تنفيذها.
- **الحل:** إضافة HTML entity escaping قبل إدراج القيم، أو إزالة `exportToCSV` إن لم يعد مستخدمًا.

### H5. Missing migration لـ `annualLeaveBalance`
- **الملف:** `src/database/db.ts` — السطور 52-64
- **الوصف:** العمود `annualLeaveBalance` موجود في `schema.ts` (سطر 207) لكن لا يوجد `ALTER TABLE` له في `db.ts`. قواعد البيانات القديمة لن تحتوي على هذا العمود.
- **التأثير:** خطأ في الاستعلامات التي تشير إلى هذا العمود
- **الحل:** إضافة `ALTER TABLE employees ADD COLUMN annualLeaveBalance REAL DEFAULT 0`.

### H6. لا يوجد `beforeunload` للحماية من فقدان البيانات
- **الملف:** `src/App.tsx`
- **الوصف:** عند إعادة تحميل الصفحة، أي تغييرات في قائمة انتظار الحفظ (save queue) تُفقد. لا يوجد تحذير للمستخدم.
- **الحل:** إضافة `window.addEventListener('beforeunload', ...)` عندما يكون هناك حفظ معلق.

### H7. خاصية `updateErpState` لا ترجع Promise — لا يمكن الانتظار
- **الملف:** `src/App.tsx` — السطور 268-281
- **الوصف:** دالة `updateErpState` لا ترجع الـ Promise من `saveQueueRef`، فالمتصل لا يمكنه `await` اكتمال الحفظ.
- **التأثير:** لا يمكن للمكونات معرفة متى تم الحفظ فعليًا على الخادم.

### H8. دالة `exportSalesInvoicesExcel` تستقبل `customerName` بدلاً من `customerId`
- **الملف:** `src/utils/excelExport.ts` — السطر 772
- **الوصف:** التصدير يأخذ `customerName` (نص جاهز) بدلاً من `customerId` (معرف للربط). هذا يفقد إمكانية الربط ببيانات العملاء.
- **التأثير:** لا يمكن追踪 الفاتورة إلى العميل في ملف Excel.

### H9. `useState` بدون `lazy initializer` للقيم المحسوبة
- **الملف:** جميع المكونات
- **الوصف:** بعض المكونات تستخدم `useState(data.accounts)` بدون lazy initializer. هذا ينفذ `data.accounts` في كل render وإن لم يكن ضروريًا.
- **التأثير:** أداء أقل (تأثير ضئيل لكنه موجود في المكونات الكبيرة).

---

## 4. مشاكل متوسطة (MEDIUM)

### M1. Array index كـ `key` في 22 موقعًا
- **الملفات المتأثرة:** Sales.tsx, Treasury.tsx, GeneralLedger.tsx, JournalEntries.tsx, Inventory.tsx, Purchases.tsx, Recipes.tsx, Reports.tsx, StatementOfAccount.tsx, Dashboard.tsx, HR.tsx, UserManagement.tsx, ExportButton.tsx
- **التأثير:** مشاكل في الأداء عند إعادة الترتيب، ومشاكل في الحالة (state) عند إضافة/حذف عناصر.
- **الحل:** استخدام `id` فريد لكل عنصر أو `${item.id}-${idx}` كحل وسيط.

### M2. `saveERPData` بدون Timeout
- **الملف:** `src/services/api.ts`
- **الوصف:** دالة `fetch` بدون `AbortController`. طلب بطيء قد يعلق قائمة انتظار الحفظ للأبد.
- **الحل:** إضافة timeout 30 ثانية.

### M3. `syncTable()` — Interpolation لأسماء الأعمدة (SQL Injection محتمل)
- **الملف:** `src/database/db.ts` — السطور 82-195
- **الوصف:** أسماء الجداول والأعمدة تُدرج مباشرة في SQL. آمن حاليًا لأنها hardcoded، لكنها fragile.
- **الحل:** استخدام قائمة بيضاء (whitelist) لأسماء الأعمدة المسموحة.

### M4. `printPDF()` — إزالة iframe بدون التحقق من وجوده
- **الملف:** `src/utils/pdfExport.ts` — السطر 290
- **الوصف:** `document.body.removeChild(iframe)` بدون التحقق `document.body.contains(iframe)`. بينما `printDocument` في `printUtils.ts` تتحقق (السطر 57).
- **التأثير:** خطأ DOM محتمل عند الإزالة.

### M5. `exportToCSV` — لا يوجد BOM للعربية في Excel
- **الملف:** `src/utils/printUtils.ts` — السطر 554
- **الوصف:** ملف CSV بدون UTF-8 BOM (`\uFEFF`). Excel القديم قد لا يتعرف على الترميز.
- **التأثير:** النصوص العربية قد تظهر مشوشة.

### M6. `exportToCSV` — `click()` بدون `appendChild`
- **الملف:** `src/utils/printUtils.ts` — السطور 556-559
- **الوصف:** زر التحميل لم يُضف للـ DOM قبل الـ click. بعض المتصفحات قد تمنع التحميل.
- **التأثير:** فشل في تنزيل الملف.

### M7. `handleExportSpreadsheet` لا يزال موجودًا في Reports.tsx
- **الملف:** `src/components/Reports.tsx` — السطور ~750-813
- **الوصف:** دالة قديمة تستخدم `exportToCSV` (HTML-based .xls) ولا تزال مستوردة وقابلة للاستدعاء. تم استبدالها بـ ExcelJS لكن الكود القديم لم يُحذف.
- **التأثير:** كود ميت (dead code) قد يُسخدم بالخطأ.

### M8. `showAlert` و `showConfirm` على `window` — Typing غير دقيق
- **الملف:** `src/App.tsx` — السطور 27-31
- **الوصف:** الأنواع (types) المعلنة للـ custom events لا تتطابق تمامًا مع الاستخدام في المكونات. `showAlert` تتوقع `type?: 'info' | 'success' | 'warning' | 'danger'` لكن `window.showAlert('title', 'msg', 'error')` يُستخدم أحيانًا.
- **التأثير:** خطأ typo لن يتم اكتشافه.

### M9. لا يوجد Validation للمُدخلات قبل الحفظ
- **الوصف:** لا توجد مكتبة validation (Zod/Yup). البيانات ترسل كما هي.
- **التأثير:** `NaN` يتحول إلى `0` بصمت، `undefined` → `null`، تواريخ غير صالحة تُقبل.

### M10. رسالة الخطأ غير متناسقة في API
- **الوصف:** بعض الـ endpoints ترجع `{ error: string }` وأخرى ترجع `{ success: false, reason: string }`.
- **التأثير:** صعوبة في معالجة الأخطاء من الـ client.

---

## 5. مشاكل منخفضة (LOW)

### L1. `numberToArabicWords` — صيغة نحوية مبسطة
- **الملف:** `src/utils/printUtils.ts` — السطور 305-356
- **الوصف:** تستخدم دائمًا "جنيه" بصيغة المفرد. الأصح "جنيه واحد"، "جنيهاً"، "جنيهات" حسب العدد.

### L2. `numberToArabicWords` — لا تدعم المليارات
- **الملف:** `src/utils/printUtils.ts` — السطر 341
- **الوصف:** الأرقام ≥ 1,000,000,000 تُعامل كـ "مليون" بدلاً من "مليار".

### L3. `fmtCurrency` vs `formatCurrency` — تسمية غير متناسقة
- **الملفات:** `pdfExport.ts` (formatCurrency) vs `printUtils.ts` (fmtCurrency)

### L4. `writeCompanyHeader` — صف الاتصال بدون `vertical: 'middle'`
- **الملف:** `src/utils/excelExport.ts` — السطور 75-78

### L5. Auto-filter مفقود في 3 تصديرات
- **الملف:** `excelExport.ts` — Income Statement, Balance Sheet, Treasury Dashboard

### L6. Duplicate company profile reader في 3 ملفات
- **الملفات:** `excelExport.ts`, `pdfExport.ts`, `printUtils.ts`

### L7. `printElementById` — فشل صامت إذا العنصر غير موجود
- **الملف:** `src/utils/printUtils.ts` — السطور 418-419

### L8. `GET /api/auth/users` بدون مصادقة
- **الملف:** `src/routes/auth.ts` — السطر 127

### L9. استخدام JSON.stringify في description (تم إصلاحه في آخر تعديل)
- **الملف:** `src/components/Sales.tsx` — السطر 558

---

## 6. مشاكل التصميم (UI/UX)

### U1. `dir` غير مضبوط في بعض الحاويات العربية
- **الوصف:** بعض الحاويات تفتقر إلى `dir={isAr ? 'rtl' : 'ltr'}` مما يسبب محاذاة خاطئة للنصوص العربية.
- **الملفات المتوقعة:** Treasury (بعض الجداول)، Report (بعض الأقسام)، GeneralLedger

### U2. ألوان `bg-white` بدون `dark:` في بعض الأماكن
- **الوصف:** بعض العناصر تستخدم `bg-white` بدون `dark:bg-slate-950` / `dark:bg-slate-900`، فتظهر بيضاء في الوضع الليلي.

### U3. `text-left` ثابت بدلاً من شرطي
- **الوصف:** عدة خلايا جدول تستخدم `text-left` ثابتة بدون التحقق من `isAr`.
- **مثال:** `className="py-2 px-3 text-start"` — الصحيح استخدام `text-start` (الذي يحترم الـ dir) بدلاً من `text-left`/`text-right`.

### U4. `w-96`, `min-w-[700px]` — عروض ثابتة قد تسبب تجاوزًا
- **الوصف:** عروض ثابتة قد لا تناسب الشاشات الصغيرة أو النصوص العربية الطويلة.

### U5. `key={idx}` في 22 قائمة — مشاكل في React re-render

### U6. أزرار بدون `cursor-pointer` في بعض الأماكن
- **الوصف:** بعض الأزرار تفتقر إلى كلاس `cursor-pointer` (خاصة في المودالات القديمة).

### U7. `ExportButton` — القائمة المنسدلة قد تظهر خلف العناصر الأخرى
- **الوصف:** قائمة الـ ExportButton المنبثقة تستخدم `absolute` بدون `z-index` مرتفع.

### U8. توحيد ألوان الأزرار
- **الوصف:** بعض أزرار التصدير تستخدم `bg-emerald-600`، وأخرى تستخدم `bg-green-600`. غير متناسق.

---

## 7. سيناريوهات الاختبار اليدوي

### 7.1 اختبار حفظ البيانات
```
1. → افتح أي صفحة قابلة للتعديل (Journal Entries / Inventory / HR)
2. → أجرِ تعديلاً (أضف قيدًا/صنفًا/موظفًا)
3. → حدث الصفحة (F5)
4. → متوقع: التغييرات محفوظة
5. → كرر 10 مرات متتالية بسرعة
```

### 7.2 اختبار فقدان البيانات عند فشل الحفظ
```
1. → أوقف السيرفر (backend)
2. → أجرِ تعديلاً في الواجهة
3. → متوقع: إشعار بالفشل (حاليًا لا يوجد — هذا هو الـ bug H1)
4. → شغّل السيرفر مرة أخرى
5. → حدث الصفحة
6. → متوقع: التغييرات ضاعت (لأن الحفظ فشل)
```

### 7.3 اختبار تصدير Excel (كل الأنواع)
```
1. → افتح كل صفحة بها زر تصدير Excel
2. → صدر ملف Excel
3. → افتح الملف في Excel
4. → تحقق من:
   - شعار الشركة موجود
   - الأعمدة صحيحة وكاملة
   - التنسيقات المالية (ج.م)
   - الألوان المتناوبة للصفوف
   - الفلتر التلقائي شغال
   - صف الإجمالي صحيح
5. → كرر لـ: Reports, SalesInvoices, Inventory, Purchases,
   Treasury, HR (4 تقارير), POS (Sales.tsx)
```

### 7.4 اختبار الطباعة والـ PDF
```
1. → افتح Reports
2. → اختر Trial Balance → Printer
3. → تحقق من ظهور نافذة الطباعة
4. → كرر لـ Income Statement, Balance Sheet
5. → اختبر PDF لكل منهم
```

### 7.5 اختبار السيناريوهات الحرجة
```
1. → إنشاء فاتورة مبيعات + تصديرها
   - تحقق من وجود جميع الأعمدة (14 عمود)
   - تحقق من أن sheet التفاصيل ليس فارغًا (bug C1)
2. → إنشاء حركة مشتريات + تصديرها
3. → إنشاء سلفة موظف + تصدير HR Excel
4. → إنشاء خزينة جديدة + تصدير ملخص الخزائن
```

### 7.6 اختبار Race Conditions
```
1. → أضف 5 قيود يومية بسرعة (زر Save متتالي)
2. → أضف 10 أصناف مخزون بسرعة
3. → حدث الصفحة
4. → متوقع: جميع البيانات محفوظة (إذا فُقد بعضها، هذا bug)
```

### 7.8 اختبار الوضع الليلي (Dark Mode)
```
1. → شغّل Dark Mode
2. → تجول في جميع الصفحات
3. → تحقق من عدم وجود خلفيات بيضاء أو نصوص غير مقروءة
```

---

## 8. فحص التصديرات (Excel / PDF / Print)

### 8.1 وظائف التصدير في `excelExport.ts` (16 دالة)
| # | الدالة | السطر | الحالة |
|---|--------|-------|--------|
| 1 | `exportTrialBalanceExcel` | 176 | ✅ |
| 2 | `exportIncomeStatementExcel` | 238 | ✅ |
| 3 | `exportBalanceSheetExcel` | 374 | ✅ |
| 4 | `exportHRLeavesExcel` | 485 | ✅ |
| 5 | `exportHRLoansExcel` | 552 | ✅ |
| 6 | `exportHRPayrollExcel` | 627 | ✅ |
| 7 | `exportHRDepartmentsExcel` | 701 | ✅ |
| 8 | `exportSalesInvoicesExcel` | 769 | ⚠️ **C1/C2: شيت ثاني فارغ + colCount** |
| 9 | `exportInventoryStocksExcel` | 886 | ✅ |
| 10 | `exportWastageLogExcel` | 953 | ✅ |
| 11 | `exportPurchaseTransactionsExcel` | 1010 | ✅ |
| 12 | `exportSuppliersExcel` | 1075 | ✅ |
| 13 | `exportTreasuryStatementExcel` | 1128 | ✅ |
| 14 | `exportTreasuryCheckbookExcel` | 1203 | ✅ |
| 15 | `exportTreasuryDashboardExcel` | 1267 | ✅ |
| 16 | `exportPOSInvoicesExcel` | 1363 | ✅ |

### 8.2 وظائف PDF in `pdfExport.ts` (3 دوال بناء)
| الدالة | السطر | الحالة |
|--------|-------|--------|
| `buildTrialBalancePDF` | 336 | ✅ |
| `buildIncomeStatementPDF` | 393 | ✅ |
| `buildBalanceSheetPDF` | 472 | ✅ |

### 8.3 وظائف الطباعة في `printUtils.ts` (9 دوال)
| الدالة | السطر | الحالة |
|--------|-------|--------|
| `printDocument` | 13 | ✅ (iframe with contains check) |
| `fmtCurrency` | 284 | ✅ |
| `fmtDate` | 293 | ✅ |
| `numberToArabicWords` | 305 | ⚠️ L1/L2: نحو مبسط + لا تدعم مليارات |
| `companyHeaderHTML` | 359 | ✅ |
| `signaturesHTML` | 394 | ✅ |
| `footerHTML` | 408 | ✅ |
| `printElementById` | 417 | ✅ |
| `exportToCSV` | 481 | ⚠️ **H4: XSS + أسلوب قديم** |

### 8.4 أزرار التصدير في المكونات
| المكون | Excel | PDF | Print | الحالة |
|--------|-------|-----|-------|--------|
| Reports (Trial Balance, P&L, Balance Sheet) | ✅ ExcelJS | ✅ iframe | ✅ iframe | ✅ |
| SalesInvoices | ✅ ExcelJS | — | — | ✅ |
| Sales (POS/Cashier) | ✅ ExcelJS | — | — | ✅ (جديد) |
| Inventory (Stocks + Wastage) | ✅ ExcelJS | — | — | ✅ (جديد) |
| Purchases (Transactions + Suppliers) | ✅ ExcelJS | — | — | ✅ (جديد) |
| Treasury (Dashboard + Statement + Checkbook) | ✅ ExcelJS | — | — | ✅ (جديد — مُعاد تصميمه) |
| HR (Leaves + Loans + Payroll + Departments) | ✅ ExcelJS | — | — | ✅ (جديد — مُعاد تصميمه) |
| HR (Employees) | ✅ ExcelJS inline | — | — | ✅ |
| HR (Payslip Print) | — | — | ✅ printElementById | ✅ |
| Recipes (Print) | — | — | ✅ printElementById | ✅ |
| Vouchers | — | — | — | ❌ لا يوجد |
| ReturnsManager | — | — | — | ❌ لا يوجد |
| PayrollManager | — | — | — | ❌ لا يوجد |
| GeneralLedger | — | — | — | ❌ لا يوجد |
| StatementOfAccount | — | — | — | ❌ لا يوجد |
| PeriodClosing | — | — | — | ❌ لا يوجد |

---

## 9. فحص حفظ البيانات (Persistence)

### 9.1 آلية الحفظ
```
User Action → onUpdateXxx → updateErpState(updater) → setErpData(next) [UI update]
                                                      → saveQueueRef → POST /api/erp-data
```

### 9.2 نقاط الضعف
| النقطة | الوصف | الخطورة |
|--------|-------|---------|
| No user feedback on save failure | الأخطاء تذهب إلى console فقط | HIGH |
| No retry mechanism | لا توجد إعادة محاولة | HIGH |
| No beforeunload | فقدان البيانات عند الإعادة التحميل | HIGH |
| No debounce | كل تغيير يرسل POST كامل | MEDIUM |
| Stale closure in onAddMoneyTransaction | قد تفقد الحركات المالية | HIGH |
| saveERPData بدون timeout | طلب علاق يوقف السلسلة | MEDIUM |

### 9.3 المسارات الحرجة للحفظ
- ✅ `AccountsChart` → `onUpdateAccounts` → saves ✅
- ✅ `JournalEntries` → `onUpdateEntries` → saves ✅
- ✅ `Inventory` → `onUpdateInventory` / `onUpdateWastage` → saves ✅
- ✅ `Treasury` → `onUpdateTreasuries` / `onUpdateBankAccounts` / `onUpdateMoneyTransactions` → saves ✅
- ✅ `HR` → `onUpdateEmployees` / `onUpdateERPState` → saves ✅
- ✅ `Sales` → `onUpdateERPState` → saves ✅
- ✅ `Purchases` → `onUpdateERPState` / `onUpdatePurchases` → saves ✅
- ❌ **`Budgets`** → كان read-only. تم إصلاحه بحفظ localStorage (آخر تعديل)
- ✅ `SalesInvoices` → `onUpdateSalesInvoices` → saves ✅

### 9.4 المكونات التي لا تحفظ (Read-only)
| المكون | ملاحظات |
|--------|---------|
| `Reports` | يقرأ فقط من data |
| `GeneralLedger` | يقرأ فقط |
| `StatementOfAccount` | يقرأ فقط |

---

## ملخص الإصلاحات المطلوبة (إجمالي 7 إصلاحات حرجة + عالية)

### 🔴 يجب إصلاحها فورًا
1. `exportSalesInvoicesExcel` — إصلاح colCount + إزالة/ملء الشيت الثاني
2. إزالة أو حماية debug endpoints في auth.ts
3. إصلاح `handleRestore` في system.ts لاستخدام `db.backup()`
4. إضافة try/catch للتصديرات الأربعة (Treasury ×3, Sales ×1)

### 🟠 يفضل إصلاحها قريبًا
5. إضافة إشعار للمستخدم عند فشل الحفظ (App.tsx)
6. إصلاح stale closure in `onAddMoneyTransaction` (App.tsx)
7. إضافة `beforeunload` للحماية من فقدان البيانات

---

## 10. ملحق: العيوب المحاسبية والحسابية الكبرى (الواقع المالي للمطاعم)

لقد تم إجراء فحص ومطابقة واقعية للعمليات الحسابية بكافة المديولات ومقارنتها بالمعايير المالية والمهنية للمطاعم، وتم رصد **4 عيوب تصميمية جوهرية** في الحسابات والأجور والـ POS:

### 🔴 1. خلل عدم توازن قيد خصم المبيعات (POS Discount Ledger Imbalance)
* **الملف المعني:** [Sales.tsx](file:///c:/Users/Express/Desktop/loding-erp/src/components/Sales.tsx) (السطور 505-515)
* **الوصف:** يُخصم مبلغ خصم الوردية الإجمالي فقط من حساب مبيعات الصالة (401). إذا كانت الوردية سفري وتوصيل فقط (مبيعات الصالة = 0)، أو كان الخصم أكبر من مبيعات الصالة، يُحذف باقي الخصم محاسبياً، مما يُنتج قيداً يومياً غير متزن (المدين وهو النقدية المستلمة صافية < الدائن وهو المبيعات مسجلة بالإجمالي).
* **📊 سيناريو محاكاة الخلل بالوردية:**
  1. مبيعات سفري (Takeaway - حساب 402) = 1,000 ج.م، مبيعات صالة (Dine-In - حساب 401) = 0 ج.م.
  2. الخصم الممنوح للعملاء = 100 ج.م، النقدية المستلمة فعلياً = 900 ج.م.
  3. **القيد الصحيح المتزن محاسبياً:**
     * **من مذكورين:** حساب الصندوق 101 بـ 900 ج.م + حساب خصم المبيعات 406 بـ 100 ج.م.
     * **إلى حساب:** مبيعات السفري 402 بـ 1000 ج.م.
  4. **قيد النظام الفعلي الحالي (بعد الإصلاح):**
     * **مدين:** حساب الصندوق 1101001 بـ 900 ج.م + حساب خصم المبيعات 4106001 بـ 100 ج.م.
     * **دائن:** حساب مبيعات السفري 4102001 بـ 1000 ج.م ⬅️ متزن بنسبة 100%.
* **💡 المعالجة المحاسبية المطبقة:** تم إنشاء حساب مبيعات مخصص باسم **"خصم المبيعات والمسموحات"** بالرمز `4106001` (Sales Discounts & Allowances) كطرف مدين، مع ترحيل المبيعات بالقيم الإجمالية للدائن ليتزن القيد بالكامل.

### 🔴 2. خلل التكرار الحسابي وصافي الربح في ميزان المراجعة (Trial Balance Double-Counting)
* **الملف المعني:** [Reports.tsx](file:///c:/Users/Express/Desktop/loding-erp/src/components/Reports.tsx) (السطور 275 و 1586 والتصدير في 675 و 736)
* **الوصف:** يضيف النظام صافي ربح الفترة ديناميكياً لحساب الأرباح المرحلة (3102001) بميزان المراجعة. وبما أن الميزان يسرد الإيرادات والمصروفات مسبقاً بالتفصيل، فإن أثر الأرباح يُحسب مرتين بالجانب الدائن، فيصبح ميزان المراجعة غير متزن بشكل دائم.
* **📊 سيناريو محاكاة الخلل:**
  1. إيرادات مبيعات = 10,000 ج.م (دائن)، مصروفات رواتب = 4,000 ج.م (مدين)، نقدية = 6,000 ج.م (مدين).
  2. صافي ربح الفترة المحسوب = 6,000 ج.م.
  3. **الميزان الصحيح المتزن:** 
     * **الجانب المدين:** (6000 نقدية + 4000 مصروف = 10,000 ج.م).
     * **الجانب الدائن:** (10,000 ج.م مبيعات).
  4. **ميزان النظام الفعلي (غير متزن):**
     * **الجانب المدين:** (6000 نقدية + 4000 مصروف = 10,000 ج.م).
     * **الجانب الدائن:** (10,000 مبيعات + 6,000 أرباح مرحلة = 16,000 ج.م) ⬅️ غير متزن بفارق 6,000 ج.م.

### 🔴 3. خلل خصم تكلفة الوجبة الإجمالية من المخزن (Recipe Inventory Over-Deduction)
* **الملف المعني:** [Sales.tsx](file:///c:/Users/Express/Desktop/loding-erp/src/components/Sales.tsx) (السطور 538-546)
* **الوصف:** يُخصم كامل سعر تكلفة الوجبة المحسوب (الذي يشمل خامات + عمالة + تعبئة + تشغيل) من حساب "مخزن الخامات 104" لتكلفة الأغذية `501`. هذا يُخفض المخزن بقيم وهمية (قيم الأجور والخدمات) مما يعجل بظهور رصيد المخزن بالسالب وهمياً وتكرار احتساب تكلفة العمالة.
* **📊 سيناريو محاكاة الخلل:**
  1. وجبة تكلفتها الكلية 65 ج.م (50 ج.م خامات + 10 ج.م عمالة + 5 ج.م تعبئة).
  2. تم بيع 10 وجبات. التكلفة الحقيقية للخامات الخارجة مادياً = 500 ج.م.
  3. يخصم النظام 650 ج.م من حساب مخزن الخامات `104` ⬅️ عجز دفتري وهمي بـ 150 ج.م بالمخازن وتضخيم لتكلفة الأغذية `501`.
* **💡 المعالجة المطبقة (التحكم والتحديد الفعلي للخامات):**
  1. **فصل الخامات وتكلفتها الفعلية:** يُخصم الآن حساب المخزن وتكلفة الأغذية بقيمة الخامات الحقيقية للوصفة فقط مستبعداً أجور العمالة والتغليف.
  2. **لوحة التحكم بالخصم وتحديد الاستهلاك الفعلي:** تم إضافة قسم جديد بالكامل في شاشة الوردية يتيح للمستخدم اختيار الخامات من المخزن وإدخال الكميات المستهلكة فعلياً بالوردية (الاستهلاك الفعلي) يدوياً، مع توفير زر "استيراد خامات الوجبات" لنسخ خامات الوصفات تلقائياً وتعديلها.

### 🔴 4. تضارب معادلات أجر العمل الإضافي وعواملها (HR vs Payroll Overtime Discrepancy)
* **الملف المعني:** [HR.tsx](file:///c:/Users/Express/Desktop/loding-erp/src/components/HR.tsx) مقابل [PayrollManager.tsx](file:///c:/Users/Express/Desktop/loding-erp/src/components/PayrollManager.tsx)
* **الوصف:** شاشة الموارد البشرية تحتسب الإضافي بمضاعف 1.5 وساعات مرنة، وشاشة مسير الرواتب المعتمد تحتسب الساعة بمضاعف 1.0 (عادي) وتفترض ثبات ساعات العمل بـ 8 ساعات دائماً، بينما تعرض واجهة جدول الرواتب أرقاماً مبنية على 240 ساعة ثابتة شهرياً.
* **📊 سيناريو محاكاة الخلل لموظف (6,000 ج.م راتب، 26 يوم عمل، 10 ساعات إضافي):**
  1. **الحسبة الأولى (HR Excel):** معدل الساعة = 28.85 ج.م × 1.5 مضاعف = 43.27 ج.م/ساعة ⬅️ الإضافي = **432.70 ج.م**.
  2. **الحسبة الثانية (HR UI):** يفترض 240 ساعة ثابتة: 25 ج.م × 1.5 = 37.5 ج.م/ساعة ⬅️ الإضافي = **375.00 ج.م**.
  3. **الحسبة الثالثة (Payroll Manager الدفاتر):** معدل الساعة بضرب 1.0 وبـ 8 ساعات يومياً = 28.85 ج.م/ساعة ⬅️ الإضافي = **288.50 ج.م**.
  4. **النتيجة:** تظهر 3 قيم مختلفة لنفس الموظف ونفس الشهر، ويتم الصرف الفعلي بقيمة أقل بكثير من المعروض بالواجهة.

---

### 💡 10.5 دليل خطوات الاختبار العملي بالواجهة (رسمياً بالأسماء العربية للتجربة اليدوية)

للقيام بتجربة واختبار هذه الإصلاحات عملياً داخل واجهة نظام LODing ERP المفتوحة أمامك الآن دون وقوع أي خطأ في التسميات، يرجى اتباع الدليل التالي خطوة بخطوة:

#### 1️⃣ اختبار ميزان المراجعة (Trial Balance Test)
- **الهدف:** التأكد من اتزان ميزان المراجعة واختفاء شريط التحذير الأحمر.
- **خطوات التجربة:**
  1. اذهب إلى قائمة البرنامج الجانبية واضغط على تبويب **"التقارير"**.
  2. من التبويبات العلوية، اختر تبويب **"ميزان المراجعة"**.
  3. اضبط فلتر التاريخ بالأعلى (تاريخ البداية والنهاية) لتشمل أي فترة بها قيود (مثلاً الشهر الحالي).
  4. انزل لآخر الجدول وانظر إلى صف **"المجموع"**:
     - ستجد أن القيمة في خانة **"مدين"** مساوية تماماً للقيمة في خانة **"دائن"**.
     - ستلاحظ اختفاء شريط التحذير الأحمر الذي كان يظهر بعبارة **"ميزان المراجعة غير متزن"**، وتظهر بدلاً منه عبارة التأكيد الخضراء.

#### 2️⃣ اختبار خصم الوردية بالـ POS (POS Discount Test)
- **الهدف:** التأكد من توازن القيد اليومي لوردية الكاشير عند وجود خصومات وتسجيل الخصم كقيد مدين مستقل في حسابه المخصص.
- **خطوات التجربة:**
  1. اذهب إلى القائمة الجانبية واضغط على تبويب **"المبيعات (كاشير)"**.
  2. في لوحة إغلاق ومطابقة الوردية، ضع القيم التالية في الحقول:
     - حقل **"مبيعات الصالة (Dine-In)"** = `0` ج.م.
     - حقل **"مبيعات السفري (Takeaway)"** = `1,000` ج.م.
     - حقل **"الخصم الممنوح للعملاء (Discounts)"** = `100` ج.م.
     - في حقول السداد بالأسفل (مثل حقل **"نقدي مستلم"** أو **"الصندوق"**) = اكتب **`900` ج.م** (وهي الصافي الفعلي بعد الخصم).
  3. اضغط على زر **"ترحيل وإغلاق الوردية"** باللون الأزرق.
  4. اذهب الآن إلى تبويب **"القيود اليومية"** في القائمة الجانبية:
     - افتح القيد الأخير المولد تلقائياً (ستجد شرح القيد: *مطابقة وتسجيل مبيعات كاشير*).
     - انظر إلى تفاصيل القيد الدائن والمدين:
       - **من مذكورين (مدين):**
         - حساب الصندوق `1101001` = **900.00 ج.م**.
         - حساب خصم المبيعات والمسموحات `4106001` = **100.00 ج.م**.
       - **إلى حساب (دائن):**
         - حساب مبيعات السفري `4102001` = **1000.00 ج.م** (بالقيمة الإجمالية قبل الخصم).
       - **النتيجة:** مجموع المدين ($900 + 100 = 1000$) = مجموع الدائن ($1000$) ⬅️ **القيد متزن تماماً بنسبة 100% ويظهر قيمة الخصم بشكل مستقل**.

#### 3️⃣ اختبار جرد وتكلفة خامات الوجبات (Recipe & Raw Materials Costing Test)
- **الهدف:** التأكد من إمكانية تغيير النسبة المئوية للتقدير التلقائي بدلاً من جمودها عند 30%، أو تحديد الخامات المستهلكة بالوردية يدوياً بالكامل للتأثير الفعلي على المخزن.
- **خطوات التجربة:**
  1. اذهب لتبويب **"المبيعات (كاشير)"** لإغلاق وردية جديدة:
     - ستلاحظ وجود كارت جديد بالكامل في الجانب الأيسر بالأسفل باسم **"خصم المواد الخام وتكلفة الأغذية"**.
     - **تجربة التقدير المرن:** اختر طريقة الخصم "تقدير تلقائي (نسبة مئوية من الإيراد)". ستلاحظ ظهور شريط تمرير (Slider) وحقل رقمي لتغيير نسبة تكلفة الطعام من (0% إلى 100%) بكل سهولة بدلاً من ثباتها عند 30%.
     - **تجربة الخصم الفعلي اليدوي:** اختر طريقة الخصم "تحديد يدوي للخامات المستهلكة (الاستهلاك الفعلي)":
       - ستتمكن من اختيار أي خامة مخزنية (مثال: لحمة، رز) وتحديد الكمية المستهلكة (مثال: `5` كجم) والضغط على زر `+` لإضافتها.
       - في حال استخدام مبيعات الأصناف (Itemized)، ستجد زر مخصص باسم **"استيراد خامات الوجبات للوردية"** يقوم بنسخ كافة الخامات المستهلكة معيارياً بناءً على وصفات الوجبات المباعة ووضعها في جدول الاستهلاك الفعلي فوراً.
       - يمكنك بعد ذلك تعديل الكميات المستوردة يدوياً بالزيادة أو النقص (لتسجيل الهدر أو العجز الفعلي) أو حذف خامات وإضافة أخرى.
  2. اضغط على زر **"ترحيل وإغلاق الوردية"** لإكمال التسجيل.
  3. اذهب لتبويب **"القيود اليومية"** وافتح القيد المولد الأخير للوردية:
     - ستجد أن حساب تكلفة الطعام وحساب مخزن المواد الغذائية تم قيدهم بقيمة تكلفة الخامات المسجلة يدوياً بالقرش.
     - اذهب لتبويب **"المخزون"** وسوف تلاحظ خصم الكميات الفعلية التي أدخلتها بدقة تامة من أرصدة الأصناف.

#### 4️⃣ اختبار العمل الإضافي بالرواتب (Overtime Test)
- **الهدف:** التأكد من تطابق حسبة الإضافي للموظف بالقرش بين شاشة شؤون الموظفين والمسير الفعلي المصروف.
- **خطوات التجربة:**
  1. اذهب لتبويب **"الموارد البشرية"** من القائمة الجانبية:
     - اختر موظفاً نشطاً واضبط بيانات عقده كالتالي:
       - الراتب الأساسي = `6,000` ج.م.
       - أيام العمل التعاقدية = `26` يوم.
       - ساعات العمل اليومية = `8` ساعات.
       - قم بإضافة **`10` ساعات عمل إضافي** في حقل ساعات الإضافي للموظف.
     - انظر لراتب الموظف في جدول الموظفين بالواجهة أو عند تصدير شيت إكسل الموحد:
       - ستجد الإضافي المستحق للموظف = **`432.70` ج.م** (معدل الساعة العادي 28.85 ج.م × 10 ساعات × 1.5 مضاعف).
  2. اذهب لتبويب **"مسيرات الرواتب"** في القائمة الجانبية:
     - اضغط على زر **"بدء مسير رواتب جديد"**.
     - اختر الشهر والسنة الحاليين واضغط تأكيد.
     - ابحث عن نفس الموظف في جدول المسير المعروض قبل الاعتماد.
     - **النتيجة:** ستجد قيمة حقل **"أجر الإضافي"** للموظف مكتوباً فيه **`432.70` ج.م** بالتطابق التام مع شاشة الموظفين وباحتساب الـ 1.5x كاملة.